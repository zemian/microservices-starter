<template>
    <div class="row justify-content-center">
        <div class="col-8">
            <div v-if="task">
                <h2><span v-text="$t('jhipsterApp.task.detail.title')">Task</span> {{task.id}}</h2>
                <dl class="row jh-entity-details">
                    <dt>
                        <span v-text="$t('jhipsterApp.task.title')">Title</span>
                    </dt>
                    <dd>
                        <span>{{task.title}}</span>
                    </dd>
                    <dt>
                        <span v-text="$t('jhipsterApp.task.description')">Description</span>
                    </dt>
                    <dd>
                        <span>{{task.description}}</span>
                    </dd>
                </dl>
                <button type="submit"
                        v-on:click.prevent="previousState()"
                        class="btn btn-info">
                    <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
                </button>
                <router-link v-if="task.id" :to="{name: 'TaskEdit', params: {taskId: task.id}}" tag="button" class="btn btn-primary">
                    <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.edit')"> Edit</span>
                </router-link>
            </div>
        </div>
    </div>
</template>

<script lang="ts" src="./task-details.component.ts">
</script>
